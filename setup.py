from setuptools import setup, find_packages


setup(name='kaia',
      version='0.0.0',
      license='GPL-3.0',
      packages=find_packages(),
      install_requires=[
      ],
      include_package_data = True,
      zip_safe=False)
